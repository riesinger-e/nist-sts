## sts-lib

This library implements the statistical tests for RNGs and PRNGs
as defined by the NIST SP 800-22r1a.

## Implementation details

This library uses `&[u8]` to store its data - 8 bits per byte. Reading in the data is left to the user
of this library.

## Usage

TODO